**Quicksort** is a divide-and-conquer algorithm that selects a pivot and partitions the collection around it.

**After partitioning:**

Elements less than or equal to the pivot are placed to its left.

Elements greater than the pivot are placed to its right.

The pivot is in its final sorted position.

The left and right portions are sorted recursively.



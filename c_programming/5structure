//A structure combines several related variables.

struct Person {
    char name[50];
    int age;
    float height;
};

struct Person p1;
strcpy(p1.name, "John");
p1.age = 30;
p1.height = 6.1;

struct Person p2 = {"Alice", 25, 5.5};




struct Date {
    int day;
    int month;
    int year;
};

struct Employee {
    char name[50];
    struct Date birthDate;
    float salary;
};




struct Person {
    char name[50];
    int age;
    float height;
};

int main() {
    // Declare a variable of type struct Person
    struct Person p1;
    
    // Initialize the structure members
    strcpy(p1.name, "John");
    p1.age = 30;
    p1.height = 6.1;

    // Print the structure members
    printf("Name: %s\n", p1.name);
    printf("Age: %d\n", p1.age);
    printf("Height: %.1f\n", p1.height);

    return 0;
}


#include <stdio.h>

struct ServerConfig {
    char ip_address[16];
    unsigned short port;
};

int main(void) {
    struct ServerConfig server = {
        "127.0.0.1",
        8080
    };

    printf("Server: %s:%u\n",
           server.ip_address,
           server.port);

    return 0;
}

Access structure members using .: server.port
If you have a pointer to a structure, use ->:
struct ServerConfig *ptr = &server;
printf("%u\n", ptr->port);
Socket programming uses structures such as: struct sockaddr_in to store an IP address, address family and port number.
